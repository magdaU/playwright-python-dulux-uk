[pytest]
bdd_features_base_dir = features/
markers =
    smoke: fast critical path - runs on every push
    regression: full regression suite
    desktop: scenarios using the 1920x1080 viewport
    tablet: scenarios using the 768x1024 viewport
    mobile: scenarios using the 375x667 viewport
    purchase: tester purchase feature
    visualizer: visualizer experience feature
