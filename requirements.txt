# Browser automation
playwright==1.61.0
pytest-playwright==0.8.0     # adds the browser/context/page fixtures + --headed/--browser CLI flags

# Accessibility scanning (axe-core under the hood)
axe-playwright-python==0.1.8

# Linting / formatting
ruff==0.16.1

# Test runner
pytest==9.1.1

# BDD layer (Gherkin feature files -> pytest test items)
pytest-bdd==8.1.0

# Reporting (standalone plugin, do not also install allure-pytest — both
# register the same --alluredir option and pytest will refuse to start)
allure-pytest-bdd==2.16.0    # renders Gherkin steps in the Allure report, mirrors allure-cucumber7-jvm
